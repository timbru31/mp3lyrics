---
engines:
  rubocop:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  bundler-audit:
    enabled: true
ratings:
  paths:
  - "**.rb"
  - Gemfile.lock
exclude_paths: []
